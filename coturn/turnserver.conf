# TURN server configuration for self-hosted ChatCall

# Listener port for TURN/STUN
listening-port=3478

# External IP (your server's LAN IP — update for production)
external-ip=192.168.68.73

# Relay port range (must match mediasoup range for firewall)
min-port=40000
max-port=49999

# Authentication
lt-cred-mech
user=chatcall:chatcall_turn_secret
realm=chatcall

# Logging
log-file=stdout
verbose

# Security
no-multicast-peers
no-cli
fingerprint
